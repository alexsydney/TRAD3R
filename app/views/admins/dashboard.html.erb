<h4>Admin Dashboard page</h4>

<%#= link_to 'Add new user', '/users/new' %> |
<%= link_to 'Bck to main page', root_path %>

<ul>
  <%  @users.each do |user| %>
    <li>
      <em>
         <%= link_to user.email, user_path(user.id) %>
      </em>
      Name: <%= user.name%><br/>
      Coin: <%= user.coins%><br/>
    </li>
  <% end %>
</ul>
